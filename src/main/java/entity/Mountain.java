package entity;

/**
 * The type Mountain.
 */
public class Mountain extends Position {

    /**
     * Instantiates a new Position.
     *
     * @param column the column
     * @param line   the line
     */
    public Mountain(int column, int line) {
        super(column, line);
    }
}
